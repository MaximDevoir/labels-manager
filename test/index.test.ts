import nock from 'nock'
import { Probot } from 'probot'

import pushPayload from './fixtures/labels-manager-testing.push.json'

describe('Labels Manager', () => {
  it('does a thing', async () => {

  })
})
